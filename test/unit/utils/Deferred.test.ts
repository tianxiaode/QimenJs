import { Deferred } from '../../src/utils/Deferred.ts';

describe('Deferred', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(Deferred).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

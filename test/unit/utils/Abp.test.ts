import { Abp } from '../../src/utils/Abp.ts';

describe('Abp', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(Abp).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

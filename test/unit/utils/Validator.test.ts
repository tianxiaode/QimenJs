import { Validator } from '../../src/utils/Validator.ts';

describe('Validator', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(Validator).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

import { duplicates } from '../../src/utils/array/base/duplicates.ts';

describe('duplicates', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(duplicates).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

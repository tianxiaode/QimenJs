import { sample } from '../../src/utils/array/random/sample.ts';

describe('sample', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(sample).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

import { shuffle } from '../../src/utils/array/random/shuffle.ts';

describe('shuffle', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(shuffle).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

import { union } from '../../src/utils/array/set/union.ts';

describe('union', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(union).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

import { difference } from '../../src/utils/array/set/difference.ts';

describe('difference', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(difference).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

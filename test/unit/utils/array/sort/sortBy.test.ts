import { sortBy } from '../../src/utils/array/sort/sortBy.ts';

describe('sortBy', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(sortBy).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

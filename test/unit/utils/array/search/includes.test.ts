import { includes } from '../../src/utils/array/search/includes.ts';

describe('includes', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(includes).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

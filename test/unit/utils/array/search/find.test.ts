import { find } from '../../src/utils/array/search/find.ts';

describe('find', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(find).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

import { flatten } from '../../src/utils/array/collection/flatten.ts';

describe('flatten', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(flatten).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

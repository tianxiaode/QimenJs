import { groupBy } from '../../src/utils/array/collection/groupBy.ts';

describe('groupBy', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(groupBy).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

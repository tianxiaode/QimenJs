import { Cookie } from '../../src/utils/Cookie.ts';

describe('Cookie', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(Cookie).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

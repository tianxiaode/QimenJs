import { delimited-string-validator } from '../../src/utils/validation/builders/delimited-string-validator.ts';

describe('delimited-string-validator', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(delimited-string-validator).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

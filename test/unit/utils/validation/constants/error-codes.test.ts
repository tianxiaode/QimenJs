import { error-codes } from '../../src/utils/validation/constants/error-codes.ts';

describe('error-codes', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(error-codes).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

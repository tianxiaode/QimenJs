import { rule-adapter } from '../../src/utils/validation/adapters/rule-adapter.ts';

describe('rule-adapter', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(rule-adapter).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

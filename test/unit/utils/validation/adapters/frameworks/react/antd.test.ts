import { antd } from '../../src/utils/validation/adapters/frameworks/react/antd.ts';

describe('antd', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(antd).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

import { special } from '../../src/utils/validation/primitives/special.ts';

describe('special', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(special).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

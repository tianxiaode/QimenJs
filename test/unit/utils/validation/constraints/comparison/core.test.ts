import { core } from '../../src/utils/validation/constraints/comparison/core.ts';

describe('core', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(core).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

import { equality } from '../../src/utils/validation/constraints/comparison/equality.ts';

describe('equality', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(equality).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

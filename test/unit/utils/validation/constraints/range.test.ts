import { range } from '../../src/utils/validation/constraints/range.ts';

describe('range', () => {
  beforeEach(() => {
    // 测试前的准备工作
  });

  afterEach(() => {
    // 测试后的清理工作
  });

  it('should be defined', () => {
    expect(range).toBeDefined();
  });

  // TODO: 添加更多测试用例
});

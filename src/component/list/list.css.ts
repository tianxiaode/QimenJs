/**
 * List 列表组件样式 — Metro 风格
 *
 * 方角标记、紧凑布局，status 驱动颜色，markForm 驱动形状。
 * ListItemComponent 相关样式已迁移至 list-item.css。
 */

export const listCSS = `
/* List 根元素 */
.q-list {
    display: block;
    font-size: 14px;
    line-height: 1.5;
    box-sizing: border-box;
}

/* 列表容器 */
.q-list__items {
    list-style: none;
    margin: 0;
    padding: 0;
}
`;

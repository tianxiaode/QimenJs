/**
 * 从数组中随机采样一个或多个元素
 * @template T 数组元素类型
 * @param {T[]} arr 需要采样的原数组
 * @param {number} [count=1] 采样元素的数量，默认为1
 * @returns {T | T[]} 如果count为1则返回单个元素，否则返回包含采样元素的数组
 */
export declare function sample<T>(arr: T[], count?: number): T | T[];
//# sourceMappingURL=sample.d.ts.map
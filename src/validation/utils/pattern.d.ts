import { ValidationContext } from "../types";
/**
 * 核心正则匹配助手
 * @returns boolean 表示是否通过，内部自动处理错误压栈
 */
export declare const validatePattern: (value: any, regex: RegExp, context: ValidationContext, expected?: string) => boolean;
//# sourceMappingURL=pattern.d.ts.map
/**
 * template-constants.ts — 模板相关类型
 */

export interface ContentPropDef {
    nodeProp: string;
}

export interface AnimationOptions {
    duration?: number;
    easing?: string;
    fill?: FillMode;
}

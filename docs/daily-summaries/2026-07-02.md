# 2026-07-02 工作总结

## 今日完成

### 1. OAuth2 可行性分析

- 回顾项目文档（architecture、build-progress、daily-summaries）
- 分析 HTTP 管道、TokenInjector、DomainRegistrar、EventBus、Cache 等现有能力
- 结论：基于当前架构实现 OAuth2 完整流程可行，且难度不高
- 已有能力覆盖约 70%，缺少的核心环节不多

### 2. 401 处理方案讨论

- 讨论了 401 拦截是否应放入 HTTP 默认管道
- 确定采用**方案 A**：TokenRefreshHandler 由 OAuth2 包自行注册，不放入 HTTP 默认管道
- 理由：不用 OAuth2 的项目零影响，职责清晰，符合已有的拆包模式

### 3. 新增 @orbitjs/oauth2 包

- **OAuth2Manager**：核心管理器，支持三种授权模式
  - 密码模式（loginWithPassword）
  - 授权码模式（loginWithCode + authorize 跳转）
  - 客户端凭证模式（loginWithClientCredentials）
- **TokenStorage**：Token 持久化
  - MemoryTokenStorage（默认）
  - LocalStorageTokenStorage
  - SessionStorageTokenStorage
- **TokenRefreshHandler**：HTTP 管道 ALIGN 阶段扩展
  - 401 拦截 + 自动刷新 + 重试
  - 并发刷新去重（共享 Promise）
  - 防递归（isTokenRefresh 标记）
- **事件通知**：token-acquired / token-refreshed / token-expired / refresh-failed
- **自动注册**：引入 @orbitjs/oauth2 即自动注册 TokenRefreshHandler

### 4. 架构设计文档

- 新增 `docs/architecture/packages/oauth2.md`
- 包含设计理念、核心决策、API、类型、文件结构、依赖关系、并发去重、与现有方案的关系

### 5. 文档更新

- 更新 ARCHITECTURE.md：27 个包，L3 层 6 个包，统计信息同步
- 更新 build-progress/README.md：28 个包进度总览
- 新增 6 个包的构建进度文件：i18n、mime、pattern、data-processor-abp、data-processor-spring、oauth2

## 项目状态

| 指标 | 数值 |
|------|------|
| 总包数 | 27 |
| 测试套件 | 207 |
| 测试用例 | 2354 |

## 包层级变化

- L3 层新增 `@orbitjs/oauth2`（依赖 http, registry, events, cache）
- OAuth2 是现有 Token 管理方案的上层封装，不替代底层 DomainRegistrar.updateToken() + TokenInjector

### 6. 全栈示例方案设计

- 讨论搭建完整全栈示例来验证 OrbitJS 各包功能是否可行
- 示例需求：Node.js OAuth2 认证服务 + 模拟 ABP API + 模拟 Spring API + 前端多域数据获取
- 确定目录结构：`examples/full-stack/`，独立 package.json
- 三个后端服务端口分离：auth-server(:3000)、abp-api(:3001)、spring-api(:3002)
- 前端用 Vite + 原生 TS，通过别名映射到 `src/` 目录
- 一键启动脚本

**待执行**：搭建示例项目代码

### 7. 全栈示例搭建与 EntityManager 集成

- 完成 `examples/full-stack` 示例项目搭建
  - auth-server（:3000）：OAuth2 认证服务
  - abp-api（:3001）：模拟 ABP 后端（PagedResultDto 格式）
  - spring-api（:3002）：模拟 Spring 后端（Page\<T\> 格式）
  - client：前端应用（Vite + 原生 TS）
  - 一键启动脚本

- 重构 client 端数据获取方式：HttpClient 直接调用 → EntityManager 能力系统
  - 新增 `client/src/domains/` 目录：统一定义各域 Schema
    - `abp.ts`：UserSchema、ProductSchema
    - `spring.ts`：OrderSchema、ItemSchema
  - 新增 `client/src/managers/` 目录：定义各域 EntityManager
    - `abp.ts`：AbpUserManager、AbpProductManager（继承 RemoteCrudEntityManager）
    - `spring.ts`：SpringOrderManager、SpringItemManager
  - 修改 `config.ts`：注册 Schema 到 SchemaRegistrar
  - 修改 `dashboard.ts`：通过 EntityManager.list()/create() 获取数据

### 8. FlatRemoteEntityState 运行时缺陷修复

**问题**：EntityManager.list() 返回 undefined，导致 `items.map is not a function`

**根因 1**：`FlatRemoteEntityState` 缺少关键方法
- `updateData(list, total)`：FlatRemoteListAbility 调用但不存在
- `toParams()`：未声明 StateSearchAbility，方法缺失
- `updateItem(item)`：RemoteGetAbility/RemoteCreateAbility 调用但不存在
- `isValidPage(page)`：FlatRemoteQueryAbility 调用但不存在

**修复**：
- 添加 `StateSearchAbility` 到 abilities 声明
- 实现 `updateData(list, total)`：更新 items、total、pages、hasMore
- 实现 `updateItem(item)`：更新单个实体并同步到 items
- 实现 `isValidPage(page)`：验证页码有效性
- 添加 `pageSizes` 属性和 `toParams` 类型声明

**根因 2**：`FlatRemoteListAbility` 防抖导致返回 undefined
- `list()` 使用 `this.debounce(...)(false)` 模式
- `debounce` 在 `immediate=false` 时，fn 在 setTimeout 中延迟执行，不返回值
- 因此 `async list()` 返回 `Promise<undefined>`

**修复**：
- `list()` 和 `refresh()` 直接调用 `_internalList()`，不再通过防抖函数获取返回值
- 防抖功能由 `BaseEntityManager.fetch()` 的 loading 状态锁保护（RemoteCreateAbility 已有此机制）

**反思**：单元测试通过但运行时失败，原因是测试用 mock state 而非真实 FlatRemoteEntityState 实例，未覆盖集成路径

## 明日计划

- 搭建 examples/full-stack 全栈示例
  - auth-server：OAuth2 认证服务（密码/授权码/客户端凭证/刷新）
  - abp-api：模拟 ABP 后端（PagedResultDto 格式）
  - spring-api：模拟 Spring 后端（Page\<T\> 格式）
  - client：前端应用（配置三域 + OAuth2 + 数据获取）
  - 一键启动脚本

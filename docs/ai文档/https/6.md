为方便后续工作和扩展，定义全局的 `types.ts` 文件是非常有用的，它可以集中存放项目中的公共类型定义，确保类型的一致性和简化维护。我们可以在各个文件夹中引入公共的类型定义，确保不同模块间的协作不容易出错。

### 目录结构更新：

```text
src/http/
├── adapter/                           # 后端适配器
│   ├── BackendAdapter.ts              # 后端适配器基类
│   ├── RestAdapter.ts                 # 适配 REST API
│   └── index.ts                       # 集中导出所有适配器
├── core/                              # 核心功能
│   ├── HttpClient.ts                  # 核心 HTTP 请求客户端
│   ├── HttpError.ts                   # 错误处理类
│   ├── HttpRequest.ts                 # 请求参数封装
│   ├── HttpResponse.ts                # 响应数据封装
│   ├── RetryPolicy.ts                 # 重试策略
│   ├── index.ts                       # 核心功能集中导出
│   └── types.ts                       # 全局类型定义
├── middleware/                        # 中间件
│   ├── AuthMiddleware.ts              # 身份验证中间件
│   ├── RetryMiddleware.ts             # 重试中间件
│   └── index.ts                       # 集中导出所有中间件
├── transport/                         # 请求传输方式
│   ├── FetchTransport.ts              # Fetch 请求封装
│   ├── XhrTransport.ts                # XHR 请求封装
│   ├── HttpTransport.ts               # 基本的 HTTP 请求封装
│   ├── HeaderMiddleware.ts            # 请求头中间件
│   └── index.ts                       # 集中导出所有传输方式
├── pipeline/                          # 错误解析管道
│   ├── ErrorParser.ts                 # 错误解析器接口
│   ├── ErrorParserFactory.ts          # 错误解析器工厂类
│   ├── ErrorParserPipeline.ts         # 错误解析管道类
│   ├── JsonErrorParser.ts             # JSON 错误解析器
│   ├── CustomBackendErrorParser.ts    # 自定义后端错误解析器
│   ├── MySpecialErrorParser.ts        # 特定场景错误解析器
│   └── index.ts                       # 错误解析管道集中导出
└── index.ts                           # HTTP 模块的入口文件
```

### 1. **`types.ts` — 定义全局类型**

为了保持一致性，我们可以在 `core/types.ts` 中定义一些通用类型，包括 `HttpRequest`、`HttpResponse`、`HttpError`，以及后续可能会用到的错误解析器相关类型。

#### **`src/http/core/types.ts`**：

```typescript
// 请求方法类型
export type HttpMethod = 'GET' | 'POST' | 'PUT' | 'DELETE' | 'PATCH';

// 请求头类型
export interface HttpHeaders {
  [key: string]: string;
}

// 请求体类型
export interface HttpBody {
  [key: string]: any;
}

// HttpResponse 类型定义
export interface HttpResponse {
  statusCode: number;
  headers: HttpHeaders;
  body: any;  // 可扩展为更细化的类型
  getBody(): any;
  isJsonResponse(): boolean;
  isCustomBackendError(): boolean;
}

// HttpRequest 类型定义
export interface HttpRequest {
  url: string;
  method: HttpMethod;
  headers: HttpHeaders;
  body?: HttpBody;
}

// 错误解析器接口定义
export interface ErrorParser {
  parse(response: HttpResponse): HttpError | null;
}

// 错误处理类定义
export class HttpError extends Error {
  constructor(
    public statusCode: number,
    public message: string,
    public data?: any
  ) {
    super(message);
    this.name = 'HttpError';
  }
}
```

---

### 2. **`ErrorParser` 接口和相关类**

`ErrorParser.ts` 是错误解析器的接口文件，定义了解析器的基本结构，以及 `ErrorParserPipeline` 管道的处理方式。

#### **`src/http/pipeline/ErrorParser.ts`**：

```typescript
import { HttpResponse, HttpError } from '../core/types';

// 错误解析器接口
export interface ErrorParser {
  parse(response: HttpResponse): HttpError | null;
}
```

#### **`src/http/pipeline/ErrorParserFactory.ts`**：

```typescript
import { ErrorParser } from './ErrorParser';
import { HttpResponse, HttpError } from '../core/types';
import { ErrorParserPipeline } from './ErrorParserPipeline';

export class ErrorParserFactory {
  private static pipeline = new ErrorParserPipeline();

  // 注册自定义解析器
  static registerParser(parser: ErrorParser): void {
    this.pipeline.addParser(parser);
  }

  // 使用管道执行错误解析
  static parseError(response: HttpResponse): HttpError {
    return this.pipeline.parse(response);
  }
}
```

#### **`src/http/pipeline/ErrorParserPipeline.ts`**：

```typescript
import { ErrorParser } from './ErrorParser';
import { HttpResponse, HttpError } from '../core/types';

export class ErrorParserPipeline {
  private parsers: ErrorParser[] = [];

  // 向管道中添加解析器
  addParser(parser: ErrorParser): void {
    this.parsers.push(parser);
  }

  // 按顺序逐个解析
  public parse(response: HttpResponse): HttpError {
    for (const parser of this.parsers) {
      const error = parser.parse(response);
      if (error) {
        return error;
      }
    }
    return new HttpError(response.statusCode, "Unknown error");  // 默认错误
  }
}
```

---

### 3. **具体的错误解析器实现**

我们为常见的错误格式提供了几个具体的错误解析器实现。可以根据项目需求继续扩展。

#### **`src/http/pipeline/JsonErrorParser.ts`**：

```typescript
import { ErrorParser } from './ErrorParser';
import { HttpResponse, HttpError } from '../core/types';

export class JsonErrorParser implements ErrorParser {
  parse(response: HttpResponse): HttpError | null {
    if (response.isJsonResponse()) {
      const body = response.getBody();
      if (body.errorCode && body.errorMessage) {
        return new HttpError(response.statusCode, body.errorMessage, body);
      }
    }
    return null;
  }
}
```

#### **`src/http/pipeline/CustomBackendErrorParser.ts`**：

```typescript
import { ErrorParser } from './ErrorParser';
import { HttpResponse, HttpError } from '../core/types';

export class CustomBackendErrorParser implements ErrorParser {
  parse(response: HttpResponse): HttpError | null {
    const body = response.getBody();
    if (body.code && body.msg) {
      return new HttpError(response.statusCode, `Custom error: ${body.msg}`, body);
    }
    return null;
  }
}
```

#### **`src/http/pipeline/MySpecialErrorParser.ts`**：

```typescript
import { ErrorParser } from './ErrorParser';
import { HttpResponse, HttpError } from '../core/types';

export class MySpecialErrorParser implements ErrorParser {
  parse(response: HttpResponse): HttpError | null {
    const body = response.getBody();
    if (body.specialErrorCode) {
      return new HttpError(response.statusCode, `Special error: ${body.specialErrorMessage}`, body);
    }
    return null;
  }
}
```

---

### 4. **HttpClient 及其他核心功能**

#### **`src/http/core/HttpClient.ts`**：

```typescript
import { HttpRequest, HttpResponse } from './types';
import { ErrorParserFactory } from '../pipeline/ErrorParserFactory';
import { HttpTransport } from '../transport/HttpTransport';

export class HttpClient {
  constructor(
    private transport: HttpTransport,
    private middleware: Function[] = []
  ) {}

  public async request(request: HttpRequest): Promise<HttpResponse> {
    // 可以根据需要通过中间件修改请求
    const modifiedRequest = this.applyMiddleware(request);

    // 发送请求
    const response = await this.transport.send(modifiedRequest);

    // 错误处理
    if (response.statusCode >= 400) {
      throw ErrorParserFactory.parseError(response);
    }

    return response;
  }

  private applyMiddleware(request: HttpRequest): HttpRequest {
    // 这里可以依次执行中间件
    return request;
  }
}
```

---

### 5. **初始化和使用**

在 `index.ts` 中，我们可以进行初始化工作，配置错误解析管道，初始化 `HttpClient`，并执行请求。

#### **`src/http/index.ts`**：

```typescript
import { HttpClient } from './core/HttpClient';
import { FetchTransport } from './transport/FetchTransport';
import { JsonErrorParser } from './pipeline/JsonErrorParser';
import { CustomBackendErrorParser } from './pipeline/CustomBackendErrorParser';
import { ErrorParserFactory } from './pipeline/ErrorParserFactory';

// 注册错误解析器
ErrorParserFactory.registerParser(new JsonErrorParser());
ErrorParserFactory.registerParser(new CustomBackendErrorParser());

// 初始化 HTTP 客户端
const
```
